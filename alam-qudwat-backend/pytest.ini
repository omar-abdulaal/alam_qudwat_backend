[pytest]
# Windows sandboxing in this dev environment can deny access to the default
# %TEMP%\pytest-of-<user> directory; keep pytest's tmp_path fixture inside
# the repo instead, where it's reliably writable.
addopts = --basetemp=.pytest_tmp
